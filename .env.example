# Copiez ce fichier en .env
# Exemple : cp .env.example .env

# Postgres
POSTGRES_DB=appdb
POSTGRES_USER=appuser

# Intentionnellement laissé pour l'évaluation :
# - en production, ne pas stocker un password sensible ici
# - on attend une migration vers secrets
POSTGRES_PASSWORD=change-me

# Backend
API_PORT=3000
NODE_ENV=development

# Frontend (utilisé par Vite)
# Intentionnellement incorrect au départ dans le code : à corriger
VITE_API_BASE_URL=http://localhost:3000
